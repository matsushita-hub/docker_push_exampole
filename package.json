{
  "name": "sfcc-mock-project",
  "version": "1.0.0",
  "description": "Mock project for Jenkins CI verification",
  "scripts": {
    "lint": "echo 'Running ESLint... OK!' && exit 0",
    "test": "echo 'Running Unit Tests... All tests passed!' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
